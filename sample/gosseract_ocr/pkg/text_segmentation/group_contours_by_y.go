package text_segmentation

/*
import(
	"fmt"
	"gocv.io/x/gocv"
)

func GroupContoursByY(contours *cv.Mat) () {
	if contours == nil {
		return nil, fmt.Errorf("input contours is nil")
	}
	defer contours.Free()

	var tableRows []optic.TextBlock
	for _, cnt := range contours {
		isHorizontal := true
		if len()
	}
}
*/
